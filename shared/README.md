# shared/

Reserved for code shared between `client` and `server` in a future phase
(e.g. shared TypeScript types for API payloads). Phase 1 keeps the client
and server as independent TypeScript projects to avoid unnecessary build
complexity — this directory exists to keep that door open without forcing
it now.
